pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "nortix-minecraft-verification"
include("contracts", "paper", "velocity", "identity-verifier")
