# Minecraft integrations

The first release does not ship a Minecraft plugin or client mod. This directory reserves the
integration surface for future Paper/Fabric/Forge implementations. Shared wire contracts live in
`packages/plugin-sdk`; the backend currently accepts signed events and a local simulator can submit
them.
